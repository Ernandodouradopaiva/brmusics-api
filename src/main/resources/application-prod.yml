# Produção — variáveis em config/brmusics-api.prod.env
spring:
  config:
    import: optional:file:config/brmusics-api.prod.env[.properties]

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
